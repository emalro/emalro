---
import ThemeToggle from "./ThemeToggle.astro";
import LanguageSelector from "./LanguageSelector.astro";

const links = [
  { id: "hero", label: { es: "Inicio", en: "Home" }, placeholder: true },
  { id: "experiencia", label: { es: "Experiencia", en: "Experience" }, placeholder: true },
  { id: "educacion", label: { es: "Educación", en: "Education" }, placeholder: true },
  { id: "proyectos", label: { es: "Proyectos", en: "Projects" }, placeholder: true },
  { id: "cursos", label: { es: "Cursos", en: "Courses" }, placeholder: true },
] as const;
---

<header
  class="sticky top-0 z-40 h-16 border-b border-border bg-bg-base/85 backdrop-blur"
>
  <div class="mx-auto flex h-full max-w-6xl items-center gap-3 px-4 md:px-8">
    <a
      href="#hero"
      class="font-mono text-sm font-bold tracking-tight text-accent"
      aria-label="Emanuel Romero"
    >
      er<span class="text-ink-primary">/</span>portfolio
    </a>
    <nav class="ml-4 hidden flex-1 items-center gap-1 md:flex">
      {
        links.map((link) => (
          <a
            href={link.placeholder ? "#" : `#${link.id}`}
            class="rounded-md px-3 py-2 text-sm text-ink-secondary hover:bg-bg-surface hover:text-ink-primary"
            data-i18n={JSON.stringify(link.label)}
            data-anchor-id={link.id}
            data-anchor-placeholder={link.placeholder}
          >
            {link.label.es}
          </a>
        ))
      }
    </nav>
    <div class="ml-auto flex items-center gap-2">
      <LanguageSelector />
      <ThemeToggle />
    </div>
  </div>
</header>
